工作总结

[done]
1. 修复 sinker 可能 save 空 message 的问题并提交代码；
2. 补充完善 receiver 端的测试，覆盖多个 sinker 保存、特殊 payload 等场景；
3. 了解到 grpc 的连接是懒连接：即使客户端无法与服务端通信，也不会立即返回错误，等到第一次 rpc 调用时才会体现出来（此时才真正建立 tcp 连接）；

[doing]
完善 probe 端的测试，增加以下场景：
1. 验证 NewReporter() 函数路由功能正确，能够通过配置信息创建 grpc reporter；
2. 验证在 client 创建连接失败后，外部接口 Post() 可以捕获错误并且返回；

[todo]
1. 提交测试文件的 pr；
2. 验证 probe 到 receiver 端到端的通信能力（待 server 代码合并后）；
3. 收集测试结果，整理测试报告。