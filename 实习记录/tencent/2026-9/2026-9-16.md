工作总结

[done]
1. 基于源码串通了 analysis 完整流程（同步→Scan→Pop-Switch→切换执行），定位每个 API 调用在流程中的位置；绘制了流程图；
2. 理清 dbha_test 的配置文件，初步构思 mock server 的启动方式，当前判断需要先于每个测例的集群重置，通过流水线执行脚本启动；

[doing]
1. 继续收敛 mock server 的实现方案，整理成文档，目前暂定为三个模块：测试机上测例的集群配置解析，模拟 dbm 的内部状态机，handler 暴露 api；
2. 整理各测例配置模板；定下 mock dbm 的 metadata 数据模型；